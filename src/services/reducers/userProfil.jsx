/* import { } from "../actions/ingredients.jsx"; */

export const userProfilReducer = {
  load: false,
  error: null,
  ingredients: [],
}

/* export const burgerIngredients = (state = userProfilReducer, action) => {
  switch (action.type) {
    case : {
      return {
        ...state,
        load: true
      };
    }
    case : {
      return {
        ...state,
        ingredients: action.list,
        load: false
      };
    }
    case : {
      return {
        ingredients: [],
        error: action.error,
        load: false
      }
    }
    default: {
      return state
    }
  }
} */
